import * as assert from 'assert';
import * as client from "../../extension";
//import * as vscode from 'vscode';
//import * as serv from "../../../../server/src/server";

describe('Suite', function() {
	describe('Client', function () {
	  it('This is where unit test of the client go', function () {
			assert.equal(1,1);

	  });
	});	
});